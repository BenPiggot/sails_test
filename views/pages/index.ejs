<div class="container" ng-app="SailsApp">
  <div ng-controller="NavCtrl" style="background: black; padding: 10px; margin-bottom: 10px; color: white">
  <div class="row">
    <div  class="col-md-6 pull-left">
      <a href="/">Home</a> |
      <a href="/about">About</a>
    </div>
    <div class="col-md-6 pull-right">

      <a href="#" ng-click="showLogin()" ng-hide="currentUser">Login</a>
      <span ng-show="currentUser">{{currentUser.firstName}}</span>
       <a href="#" ng-click="logout()" ng-show="currentUser">Logout</a>

    </div>
  </div>
  </div>
  <alerts></alerts>
  <ng-view></ng-view>
